import {defineSchema} from 'convex/server';

// Tables are added in later phases (agents, instances, delegations,
// revocations, elevationRequests, tenantPolicies, auditLog, jwksCache).
export default defineSchema({});
